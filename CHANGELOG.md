# 更新记录

## 1.6.0

给上一轮补的每个母题**再造两个以上同类变体**。原则是跟着世界书的生成逻辑走，不跨态乱凑——比如"猎物描写三件套"分明是僵住/发抖/认命三态，变体就必须在各自态内换喻体。

词元变体（字面量 801 → **883** 条）：

- **生理反应固定库**四态各补 2–4 条：血液凝固、心脏像被攥住、喉咙发不出声音、瞳孔一瞬间收缩／后槽牙咬紧、指关节捏得发白、指关节捏得咔咔响／尾椎一麻、后颈泛红、小腹收紧、耳根烧起来／胸口像被掏空、喉咙堵住、眼眶发酸、浑身没了力气
- **猎物三态**：僵住补脊背瞬间僵直、呼吸停滞了半拍、双脚像生了根、身体像被冻住；发抖补从脚底一路抖到头顶、细密的战栗顺着皮肤爬开、像筛糠一样抖；认命补像被抽走了所有力气、整个人软了下去、像断了线的木偶
- **狩猎三件套**：观察补估量着猎物的弱点、视线像在切割、目光像在标记；等待补不急于下手、像在等猎物自己投降、不慌不忙地收网；收割补像猛兽锁定猎物、终于收网、缓慢而不容抗拒地
- **对话前置三套**：手指／目光／气息各补 3–4 条
- **五感**：撞进眼底、扑进鼻腔、在耳膜上震动、萦绕在鼻端、铁锈味、甜腥、潮气、腥气
- **动机模板**：某种更原始的东西在作祟、本能在叫嚣、兽性在苏醒、烙印在骨血里、连他自己都分不清
- **逢 X 必 Y**：偏偏在此时、就在这个节骨眼上、像是嫌不够似的、仿佛还嫌不够、一阵酥麻窜过、电流般的战栗
- **时间词缀**：彼时彼刻、正当此时、就在那一瞬、许多年后、多年以后、是夜
- 声音矛盾（低沉而富有磁性／银铃般清脆／奶声奶气）、复读机（更准确地说／更确切地说／或者说）

句式变体（句式 115 → **131** 条）：

- **对比衍生**：与之前/方才/先前……不同、和……别无二致/如出一辙、一如/犹如/恰如……那般
- **全知衍生**：无人知晓的是、谁也没有注意到、没有人发现 ／ 在某个看不见的地方、在视野之外 ／ 从高处望去、从上方看下去
- **命运衍生**：命运/宿命 + 织线/丝线/罗盘/骰子/牌局/帷幕/笔触/轮盘/钟摆；命运开始收网、宿命终于露出真容
- **结构化套路衍生**：像一柄/一根/一张……刺入/钉进/罩下来；身体像一根绷紧的弦、身体像被抽空
- **从 A 涌向 B 衍生**：自……向……、顺着/沿着/贴着……爬上来
- **时间停滞衍生**：四周失声、安静得能听见心跳、被拉成永恒、连呼吸声都消失
- **对话后缀衍生**：语气里满是……、嗓音浸着……（动词扩到 带着/透着/压着/裹着/含着/藏着/浸着/淬着/黏着/满是/尽是/全是）
- **声音矛盾**、**空转动词衍生**（使得……多了几分/平添）、**反问衍生**（对吧？/何尝不是？/又如何呢？）、**段首衍生**（说来也怪/有意思的是/奇怪的是/不知怎的）

去重与合并：

- `san-die-de` 用重复组改写成"三叠及以上"，`panduan-shi-de` 改写成"两折及以上"，不再为同一现象并列一条新句式
- 删掉与 `zai-zhe-de-li`、`kongzhuan-dongci` 同构重复的 `zai-manshi`、部分 `kongzhuan-dongci2` 分支
- 修正 `quanzhi-buzhi2`：原来强制要求"…的是"后缀，导致「谁也没有注意到」不匹配

试过但**撤掉**的优化：

- 给句式加"字面前缀预筛"（先用 indexOf 判断前缀在不在，不在就跳过整条正则）。写完后实测：40 KB 普通文档 15.0ms → 14.4ms，102 KB 纯英文代码 1.7ms → 1.6ms，**零收益**（V8 正则本身就有前缀快速失败）。它还因为没处理顶层择一分支而漏报过 5 条。结论：60 行复杂代码换 0 收益，撤掉

测试：`test/run.js` 145 项，新增 26 条衍生句式行为验证 + 18 组母题"至少两条变体"的分态校验。

## 1.5.0

**规则级审计**：查出并补齐"世界书要求 AI 产出、但没给固定词表"的规则。

前两轮审计管的是"词"（世界书里的短语收全没有、用户点名的词在不在）。这一轮换了视角管"规则"：把四条世界书的 197 条规则逐条过一遍，看每条是否至少有一个可被引擎识别的表面形式。词表核对有个盲区——**只给行为描述或示例句、不给词表的规则，一条短语都不产生，于是永远不会被报缺**。

审计结果：197 条规则里，181 条有可识别词面，16 条确属跨句语义（段落首尾呼应、整句重复等）无法靠词面判断，已进显式清单并逐条写明理由。

补的缺口（举其要者）：

- **第一版第 1 条形副词族**：无法言喻、细若蚊蝇、空洞的、低哑的、黏腻的、沉甸甸的、亮晶晶的、直勾勾的、死死地、紧紧地、彻底地、无意识地、习惯性地
- **第一版第 13 条生理反应固定库**：心脏骤停、指尖冰凉、喉头发紧、瞳孔骤缩、指节攥得发白、尾椎窜上一股酥麻、脊背过电般、小腹一紧、胸腔里像破了个洞、喉咙哽住、四肢百骸都被抽空
- **第一版第 24 条猎物描写三件套**：脊背绷成一张弓、呼吸漏了一拍、四肢像被钉住、从尾椎骨一路抖上来、细密的颤栗、像被抽走了所有骨头
- **第一版第 23 条狩猎场景三件套**：丈量猎物的弱点、不急着收网、像捕食者、像是剥开
- **第一版第 32 条对话前置动作**：指尖敲了敲桌面、指腹捻过杯沿、抬了抬眼皮、吐出一口气、呼吸沉了沉
- **第一版第 19 条五感同步**：落在眼底、在耳边萦绕、钻入鼻腔、腥甜
- **第一版第 12 条动机模板**：身为猎人猎物的本能、刻在骨子里的、近乎于条件反射的、连他自己也说不清
- **第一版第 11 条逢 X 必 Y**：偏偏就在这时、仿佛嫌还不够、电流窜过
- **第一版第 2 条固定量词**：几分；**第 6 条比喻引导词**：好似、宛若；**第 34 条时间词缀**：彼时、此刻、天色将暗未暗之际
- **第二版第 23 条语调突变**：他的目光在空气中停留了一瞬

新增句式（这些规则原本只给了示例句，没给词表）：

- 对比句式（第一版第 9 条 / 第四版第 47 条）：与刚才……时不同、和……几乎一模一样、也如……般
- 两叠"的"定语（第二/三版第 9 条、第四版第 13 条明说"两个足够显出敷衍"）
- 判断句（第一版第 28 条）：X 的，Y 的。
- 对话后缀（第一版第 15 条）：声音/尾音/语气 + 带着/透着/压着……
- 从 A 涌向 B（第一版第 14 条）：热度/寒意/酥麻 + 窜/烧/漫/涌……
- 对话标记：开口：

新增类别：

- **R18 · 亲密术语**（第一版第 4 条）：肠液/爱液/淫液、肉穴/蜜穴、肉刃/肉茎/阳物/那话儿。世界书把它列为独立的"按场景启用"规则，所以给成独立类别，可单独关闭

新增测试：

- `test/rule-coverage.js`：**规则级**覆盖审计（本轮的产出）。逐条检查规则是否有可识别词面，纯行为规则必须进显式清单并写明理由，不允许用清单糊弄

数据规模：字面量 733 → **801** 条，句式 108 → **115** 条，类别 12 → **13** 个。

## 1.4.0

按用户点名的词表做一轮补全，并补齐衍生句式。字面量 714 → **733** 条，句式 79 → **108** 条。

新增词条（用户指定）：

- 心理/动作：一切仿佛都慢了下来、沉默了片刻、喉头滚动、咽喉滚动、僵硬、暧昧、专业
- 结尾式：这就够了、那就够了、这样就好
- 草莓替换规则：草莓蛋糕 / 草莓马卡龙 / 草莓巧克力 / 草莓曲奇（第四版第 12 条要求把甜品描写强制替换为草莓系列）
- 动物塑补充：小猫爪子、野兽般、小兽
- 狡黠、世界突然安静了

新增衍生句式（对照世界书补全的相似套路）：

- 关联词：如果……我就……、真正的……才刚刚开始、在这……的……里、这/这种……比……都让他/她感到……
- 全知/目光：目光停留了X秒、目光落在XX身上、眼中闪过一丝……、眼中闪烁着……的光芒、那是一双怎样的……啊、看待蝼蚁一般（含 `视若/如同/仿佛蝼蚁` 变体）
- 心理/身体：心中涌起一股……、他/她知道，……、凉意/寒意从……窜/爬/冲（含变体）、身体部位发白/惨白/冷白/泛青白、僵在原地/僵住/僵硬地、悬浮/漂浮在半空中
- 声音：喉间溢出一声……、声音不大/轻却……、声音变得……、语气平淡/平平/毫无波动
- **命运/宿命变体池**：按 B 组第 10 条要求，从"齿轮"硬锁定改为 `命运/宿命/时光 + 齿轮/洪流/棋局/獠牙/轨迹/安排/巨轮/漩涡/枷锁/审判`，另加"命运的……转动/咬合/改写/露出"
- 时间停滞变体：世界突然安静了 / 按下暂停键 / 时间……停滞/静止/凝固/失重
- 动作修饰：很轻地……了一下、轻轻地……了一下、……了一下（顿/弯/停/愣/怔/晃/抖/颤/僵）、淬了/淬着X、长年……的薄茧
- 空转动词：进行了一次…… / 使得……变得……
- **破折号滥用**：punct 类别新增 `——` 检测

修复（都是这轮测试暴露出来的）：

- **句式末尾用了非贪婪通配**：`[^。]{0,n}?` 在句尾会退化成 0 字符，等于只匹配了个前缀，那条句式实际退化成它自己的字面量。新增 `GAPG`（贪婪）用于句尾通配
- **字面量分块按"整块最长成员"计权**：一档里混着 20 字和 2 字的短语时，块内短词被高估，反过来抢走本该由句式覆盖的长匹配（6 字的「眼中闪过一丝」压过 9 字的「眼中闪过……」）。改为**按长度分档打块**，块内长度接近，权重才代表真实长度
- **长度估算把择一分支累加**：`(?:命运|宿命)的?(?:齿轮|洪流|…)` 被算成 30+ 字符，抢走精确长词「命运的齿轮开始转动」。改为取**最长分支**
- 量词折算上限 8 → 4：8 的时候「命运的齿轮开始转动」仍会被「命运的…转动」抢走类别归属

新增测试：

- `test/user-terms.js` + `test/fixtures/user-requested-terms.txt`：**用户点名词表核对**，332 条逐条检查是否被字面量或句式覆盖。这是独立于世界书审计的第二道防线——世界书审计保证"原文有的都收了"，这里保证"用户点名的都在"
- `test/run.js` 增加 29 条衍生句式行为验证

## 1.3.1

配色改为高饱和，解决"高亮不够鲜艳"。

问题：为了让 token 颜色在浅色与深色底上都能读，默认取的是中间调；其中 `function` / `punct` 更是特意压成了灰色（`#6b7280`），本意是不抢重点。结果用户拿这类词测试时，看到的是"亮是亮了，但灰扑扑的"。

调整：

- 默认配色换成**高饱和鲜艳色**（`#ff5555` / `#ffb86c` / `#8be9fd` / `#50fa7b` / `#ff5fa2` / `#f1fa8c` 等），色相按色环均匀分布，12 类互相可区分
- `pattern`（关联词）加**加粗**：`fontStyle: "bold underline"`
- `function` 从灰色改为亮蓝 `#6fb8ff`，`punct` 改为亮灰 `#b0b8c4`
- 原中间调保留为「柔和」方案；命令「把配色写入用户设置」改为三选一：鲜艳（默认）/ 柔和 / 浅色主题
- README 增补完整配色表与自定义方法（含 `background` 荧光笔效果说明）

## 1.3.0

修三个导致「装了却看不到高亮」的问题。

修复：

- **词表漏收**：第四版第 12 条替换池的「程度·情态副词」整组 20 个词（不容质疑、几不可查、微不可察、毫无疑问、确实、现在、骤然…）以及另外约 80 个条目从未写入数据集。字面量从 544 条补到 **714 条**
- **原生语法默认排除了 `function` / `punct`**：用户拿这些词测试时会以为扩展没生效。现在两个引擎都默认收录全部 12 个类别，噪音类别用低饱和灰压低视觉权重
- **TextMate 规则顺序 bug**：TextMate 在同一位置有多条规则命中时取"规则顺序的第一个"，不做最长优先。字面量规则原本排在结构句式前面，导致短词「也许」抢走「也许吧。但」的起始位置，长句式在原生模式下永远匹配不上。生成语法时改为按最长可能匹配长度降序排列

新增：

- `test/coverage.js` 回归守卫：对照原始世界书逐条核对词表覆盖率，区分"真遗漏的短条目"（失败）与"规则示例/元描述"（92 项显式跳过清单，每项带理由）。这次就是靠它把遗漏全找出来的
- `test/grammar.js` 增加行为回归：短词不得截断长句式、长短语不得被短前缀截断、实测行 20 词必须全亮
- 收紧原生化语句式：`num-pupil` 不再吞掉「喉结上下滚动」这类身体反应短语
- 补齐段落结尾废句（这就是当时发生的事 / 事情就是这样 / 一切就是这样开始的）

## 1.2.0

让纯文本文件也能原生高亮。

背景：VS Code 内核注册了 `plaintext` 语言（`.txt` 等），但内置扩展里**没有任何 plaintext 的语法文件**，所以 `injectTo: ["text.plain"]` 是空转——没有语法可注入。

新增：

- 给 `plaintext` 语言贡献**根语法** `syntaxes/baguwen-plaintext.tmLanguage.json`（scopeName `source.baguwen.plaintext`）。该语言本来没有语法，我们的规则是纯增量，不覆盖任何东西
- `scripts/build-grammar.js` 改为同时产出注入语法与纯文本根语法，内容同源
- 注入目标从 6 个扩到 15 个：新增 reStructuredText、LaTeX、handlebars、日志、ini/properties/conf、dotenv、makefile、diff、git-commit
- 新增样例 `sample/八股样例.txt`，用于验证纯文本路径
- `test/grammar.js` 增加把纯文本语法当**根语法**直接分词的测试

## 1.1.0

改为以 **VS Code 原生高亮**为默认，启用即生效，不再需要按 F5。

新增：

- TextMate 注入语法 `syntaxes/baguwen.tmLanguage.json`，通过 `injectTo` 注入到已有语言
- `configurationDefaults` 提供 12 组 `baguwen.*` scope 的 token 配色，装上就有颜色
- `baguwen.engine` 配置：`native`（默认）/ `decoration` / `both`
- 命令「八股高亮: 把配色写入用户设置」，提供深色 / 浅色 / 默认三套配色
- 构建脚本 `scripts/build-grammar.js`，从词表数据生成语法，保持单一数据源
- `test/grammar.js`：真实 TextMate + Oniguruma 引擎测试，加载本机 VS Code 自带的 markdown 语法验证注入

变更：

- 原生模式下 `activate()` 提前返回：不建装饰器、不订阅事件、不扫描，运行时零开销
- 输出通道改为按需创建
- 装饰器模式保留全部原有能力（逐类别开关、忽略词、自定义颜色、波浪线、状态栏计数）

## 1.0.0

首个版本（装饰器引擎）。

- 12 个高亮类别：结构句式·关联词、陈词滥调名场面、替换池固定短语、情绪·状态词、比喻锚点·动物塑、人体·解剖词、环境模板句、对话·说字替换、狩猎·宗教·史诗·命运、伪精确数字、高频虚词、符号增殖
- 词表来自 SillyTavern 八股世界书四个版本，共 544 条字面量短语 + 79 条正则句式
- 关联词支持成对匹配：不是…而是…、与其说…不如说…、非但…反而…、介于…和…之间 等
- Aho–Corasick 单遍扫描；20 万字文本约 186 ms
- 长匹配优先去重叠，同段文字只带一种颜色
- 命令行工具 `cli.js`，与扩展共用匹配内核
