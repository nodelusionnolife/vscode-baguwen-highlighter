# 统一换行符：仓库内一律 LF，避免 Windows/Linux 协作时的 CRLF 噪音
* text=auto eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.ico binary
*.vsix binary

# 生成产物标记：在 GitHub 上默认折叠 diff
syntaxes/*.json linguist-generated=true
